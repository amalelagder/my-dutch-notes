# 1. open the file directory where the files live
1. `CTRL + R` to open the terminal
2. Write `cmd` to open the command Prompt then `enter`
3. Write `F:` to navigate to the folder
4. Write `cd my-dutch-notes` to reach the quartz file

# 2. Test everything local first
1. Write: `npx quartz build --serve`
2. Open:  http://localhost:8080
hint: exit with **ctrl+c**. You get a: **Terminate batch job (Y/N)?** 

# 3. Sync to GitHub
To sync to GitHub: npx quartz sync